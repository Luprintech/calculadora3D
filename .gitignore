# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# build / producción
frontend/dist
backend/dist

# misc
.DS_Store
Thumbs.db
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo

# google genkit cache
.genkit/*

# entorno (nunca subir al repo)
.env*
# Pero sí trackeamos el ejemplo (sin secretos reales)
!.env.example
!backend/.env.example

# base de datos local SQLite
data.db
backend/data.db
data.db-shm
data.db-wal
backend/data.db-shm
backend/data.db-wal

# archivos subidos por usuarios (logos PDF, etc.)
backend/uploads/

# script de deploy (contiene IP local del NAS)
deploy.sh

# artefactos locales y temporales
backend/*.log
nul
openspec/
